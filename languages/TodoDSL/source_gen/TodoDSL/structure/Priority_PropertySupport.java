package TodoDSL.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class Priority_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<Priority> constants = ListSequence.fromList(Priority.getConstants()).iterator();
    while (constants.hasNext()) {
      Priority constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<Priority> constants = ListSequence.fromList(Priority.getConstants()).iterator();
    while (constants.hasNext()) {
      Priority constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    Priority constant = Priority.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
